type User {
  username: String!
}

type UserList {
  data: [User]!
  currentPage: Int!
  pageSize: Int!
  total: Int!
}

type Article {
  title: String!
}

type ArticleRequired {
  title: String!
}

type Query {
  getUser: UserList!
  getArticles: ArticleRequired!
}
