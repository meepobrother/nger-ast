type Cat {
  id: Int!
  name: String!
  age: Int!
}

input CatInput {
  id: Int!
  name: String!
  age: Int!
}

type Query {
  getCats: 
  cat(id: Int!): Cat!
}

type Mutation {
  createCat(args: CatInput!): Cat!
}

type Subscription {
  catCreated: 
}
